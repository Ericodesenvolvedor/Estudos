// condition ? value1 : value2;
let pao = true;
let queijo = true;

const niceBreakFast = pao && queijo ? 'Café top' : 'Café cancelado!';
console.log(niceBreakFast);

console.clear();

// Consultando maioridade ou menor de idade
const idade = 17;

const maioridade = idade >= 18? 'Maioridade' : 'Ainda é menor de idade';
console.log(maioridade);