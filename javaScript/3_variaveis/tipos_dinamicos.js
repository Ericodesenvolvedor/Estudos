// Tipagem fraca - não precisa definir um tipo.
// O tipo é definido quando você começa a distribuir os valores.
// Com excessão do const que vai permanecer sempre com a mesma tipagem.

let clima = "quente"

console.log(clima)
console.log (typeof clima) // typeof diz o tipo da variável.

// Mudando tipagem da variável
clima = 22

console.log (clima)
console.log (typeof clima)